version: "3.7"
services:
  app:
    build : .
    ports:
      - "8080:8080"
  postgres:
    image : postgres:latest
    environment:
      POSTGRES_USER : postgres
      POSTGRES_PASSWORD : manojkasu
      POSTGRES_DB : manoj
    ports:
      - 5433:5432
